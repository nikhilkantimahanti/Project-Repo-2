public class SquareCalculator 
{
    // Method to calculate the square of a number
    public static Integer Squarecalculate(Integer no) {
        if (no != null) {
            return no * no;
        } else {
            return null;
        }
    }
}