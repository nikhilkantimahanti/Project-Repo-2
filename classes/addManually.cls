public class addManually {
    // Method to convert meters to centimeters
    public static Decimal metersToCentimeters(Decimal meters) {
        return meters * 100;
    }

    // Method to convert meters to millimeters
    public static Decimal metersToMillimeters(Decimal meters) {
        return meters * 1000;
    }
}